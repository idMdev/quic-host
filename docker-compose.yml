version: '3.8'

services:
  quic-host:
    build: .
    image: quic-host:latest
    ports:
      - "8443:8443/tcp"  # HTTP/2 and HTTP/1.1 fallback
      - "8443:8443/udp"  # HTTP/3 (QUIC)
    environment:
      - PORT=8443
    restart: unless-stopped
    container_name: quic-host
